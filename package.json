{
  "name": "firestore-rules-organizer",
  "version": "1.0.8",
  "description": "Organize firestore rules and split it to different files",
  "main": "src/cls.js",
  "scripts": {
    "start": "node ./src/cli.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "firestore",
    "rules"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concat": "^1.0.3"
  },
  "bin": {
    "build-firestore-rules": "./src/cli.js"
  }
}
